!version 0.5

[help] Build the project.
[pass] Build complete.
[fail] Build failed.
build(preset = "gcc") ? @os("linux"):
    cmake --preset {{preset}}
    cmake --build --preset {{preset}}

build(preset = "clang") ? @os("macos"):
    cmake --preset {{preset}}
    cmake --build --preset {{preset}}

build(preset = "msvc") ? @os("windows"):
    cmake --preset {{preset}}
    cmake --build --preset {{preset}}

[help] Build the project with release optimizations.
[pass] Release build complete.
[fail] Release build failed.
build-release(preset = "gcc") ? @os("linux"):
    cmake --preset {{preset}}
    cmake --build --preset {{preset}}-release

build-release(preset = "clang") ? @os("macos"):
    cmake --preset {{preset}}
    cmake --build --preset {{preset}}-release

build-release(preset = "msvc") ? @os("windows"):
    cmake --preset {{preset}}
    cmake --build --preset {{preset}}-release

[help] Run the built program.
[fail] Program failed.
run(preset = "gcc", samples = "64") ? @os("linux"):
    cmake --preset {{preset}}
    cmake --build --preset {{preset}}
    ./build/{{preset}}/Debug/kray --samples {{samples}}

run(preset = "clang", samples = "64") ? @os("macos"):
    cmake --preset {{preset}}
    cmake --build --preset {{preset}}
    ./build/{{preset}}/Debug/kray --samples {{samples}}

run(preset = "msvc", samples = "64") ? @os("windows") shell~=pwsh:
    cmake --preset {{preset}}
    cmake --build --preset {{preset}}
    build/{{preset}}/Debug/kray.exe --samples {{samples}}

[help] Run the release build.
[fail] Program failed.
run-release(preset = "gcc", samples = "512") ? @os("linux"):
    cmake --preset {{preset}}
    cmake --build --preset {{preset}}-release
    ./build/{{preset}}/Release/kray --samples {{samples}}

run-release(preset = "clang", samples = "512") ? @os("macos"):
    cmake --preset {{preset}}
    cmake --build --preset {{preset}}-release
    ./build/{{preset}}/Release/kray --samples {{samples}}

run-release(preset = "msvc", samples = "512") ? @os("windows") shell~=pwsh:
    cmake --preset {{preset}}
    cmake --build --preset {{preset}}-release
    build/{{preset}}/Release/kray.exe --samples {{samples}}

[help] Format files with clang-format.
[pass] Formatting complete.
[fail] Formatting failed.
fmt() ? @os("windows") shell~=pwsh:
    | $exts = 'cpp', 'cppm'
    |
    | foreach ($e in $exts) {
    |     Get-ChildItem -Path src -Recurse -File -Filter "*.$e" | ForEach-Object { clang-format -i $_.FullName }
    | }

fmt() shell~=bash:
    | exts="cpp cppm"
    |
    | for e in $exts; do
    |     find src -type f -name "*.$e" -exec clang-format -i {} +;
    | done

[help] Clean build outputs.
[pass] Cleaning complete.
[fail] Cleaning failed.
clean():
    rm -rf build build-release
